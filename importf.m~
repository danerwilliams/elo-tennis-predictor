%% Import data from spreadsheet
% Script for importing data from the following spreadsheet:
%
%    Workbook: /Users/danewilliams/Documents/GitRepo/elo-tennis-predictor/2019.xlsx
%    Worksheet: 2019
%
% To extend the code for use with different selected data or a different
% spreadsheet, generate a function instead of a script.

% Auto-generated by MATLAB on 2019/04/09 00:22:55

%% Import the data
function data = importf(file, sheet, range)

[~, ~, data] = xlsread('/Users/danewilliams/Documents/GitRepo/elo-tennis-predictor/2019.xlsx','2019');
Untitled = Untitled(1:800,3:11);
Untitled(cellfun(@(x) ~isempty(x) && isnumeric(x) && isnan(x),Untitled)) = {''};

idx = cellfun(@ischar, Untitled);
Untitled(idx) = cellfun(@(x) string(x), Untitled(idx), 'UniformOutput', false);

%% Clear temporary variables
clearvars idx;